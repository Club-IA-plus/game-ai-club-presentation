version: '3.8'

services:
  phaser-game:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: phaser-game-dev
    ports:
      - "8080:80"
    volumes:
      - .:/usr/share/nginx/html:ro
    restart: unless-stopped

